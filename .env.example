# PostgreSQL Database Configuration
# Sửa các giá trị bên dưới theo cấu hình PostgreSQL của bạn

DB_HOST=localhost
DB_PORT=5432
DB_NAME=graph
DB_USER=postgres
DB_PASSWORD=your_password_here
DB_SCHEMA=public

# Hướng dẫn:
# 1. Sửa DB_PASSWORD thành password thật của PostgreSQL
# 2. Chạy: python db_config.py để test connection
# 3. Nếu OK, chạy: python html_db.py để test database
